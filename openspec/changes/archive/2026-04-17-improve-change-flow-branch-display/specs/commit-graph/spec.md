## MODIFIED Requirements

### Requirement: 用户可以查看提交图

系统 SHALL 以可视化图形方式显示仓库的提交历史。

#### Scenario: 提交图布局
- **WHEN** 系统渲染提交图
- **THEN** 子节点位于父节点下方
- **AND** 并行开发线以分支形式水平分开显示
- **AND** 合并点清晰显示合并线汇聚
- **AND** 同一父变更的多个子变更以水平方向排列显示，形成清晰的分支结构
- **AND** 每个分支使用可区分的视觉样式
