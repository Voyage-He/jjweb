## 1. 修改连线绘制路径

- [x] 1.1 修改 `CommitGraph.tsx` 中的 `draw` 方法，将连线的起点从 `y + nodeRadius` 改为 `y`。
- [x] 1.2 修改 `CommitGraph.tsx` 中的 `draw` 方法，将连线的终点从 `parentY - nodeRadius` 改为 `parentY`。

## 2. 添加圆点标记渲染

- [x] 2.1 在连线绘制循环中，遍历连线经过的所有中间网格行（`intermediateY`），在计算出的对应坐标处添加绘制实心圆点的逻辑，并添加一定的透明度。
- [x] 2.2 确保圆点颜色与连线颜色一致。
- [x] 2.3 设置圆点半径为 2px（直径 4px）。

## 3. 验证与优化

- [x] 3.1 验证在各种缩放和滚动场景下圆点与连线、节点的对齐情况。
- [x] 3.2 验证在合并节点（多个父节点）和分支节点（多个子节点）处的显示效果。
