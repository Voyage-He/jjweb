## MODIFIED Requirements

### Requirement: 用户可以查看提交图
系统 SHALL 以网格布局（Table Layout）的可视化图形方式显示仓库的提交历史。

#### Scenario: 提交图布局
- **WHEN** 系统渲染提交图
- **THEN** 系统采用网格布局，将每个修订版本放置在独立的单元格中
- **AND** 子节点位于父节点下方
- **AND** 并行开发线以分支形式水平分开显示
- **AND** 合并点清晰显示合并线汇聚
- **AND** 系统 SHALL 使用水平和垂直分割线来划分修订版本和分支
- **AND** 系统应保留原本用于标识变更节点的可点击交互区域
